# Pick ONE model provider. Leave both empty to run the retrieval-only local stand-in.
GOOGLE_API_KEY=            # Gemini via Google ADK (codelab path). Get one at aistudio.google.com
ANTHROPIC_API_KEY=         # Claude via Anthropic tool runner
# Optional overrides
# SAM_RUNTIME=adk|claude|local
# GEMINI_MODEL=gemini-3.5-flash
# CLAUDE_MODEL=claude-opus-5
